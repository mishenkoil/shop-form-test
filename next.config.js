/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  reactStrictMode: true,
  basePath: '/shop-form-test',
  assetPrefix: '/shop-form-test/',
  trailingSlash: true,
}

module.exports = nextConfig
